name: Supabase Keep Alive

on:
  schedule:
    # Jalan setiap hari jam 00:00 UTC (07:00 WIB)
    - cron: '0 0 * * *'
  workflow_dispatch: # Tombol manual untuk test ping

jobs:
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Project
        # URL sudah diperbaiki (hanya satu https://)
        run: curl -I https://shbxnmjarrbbeuoccigl.supabase.co/rest/v1/